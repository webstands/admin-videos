import { createTheme } from "@mui/material";

export const appTheme = createTheme({
  palette: {
    mode: "dark",
    primary: { main: "#f5f5f1" },
    secondary: { main: "#E50991" },  
    text: { primary: "#f5f5f1" }, 
  },
});
